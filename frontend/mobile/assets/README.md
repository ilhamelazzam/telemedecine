This folder is intended to contain app icons and a splash image for the Expo app.

Place a 1024x1024 PNG for the app icon at `./icon.png` and a 1242x2436 (or similar) PNG for the splash at `./splash.png`.

Currently these files are placeholders. Replace them with production assets before building the app.
