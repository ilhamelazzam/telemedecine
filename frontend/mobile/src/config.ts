// Backend API URL (not used when USE_API is false)
export const API_URL = 'http://localhost:8000';

// Toggle to use the real backend API. Set to `false` for local testing without backend.
export const USE_API = false;
